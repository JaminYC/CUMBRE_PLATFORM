{
  "name": "@cumbre/sdk",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "description": "Reusable service SDK contracts for the CUMBRE platform",
  "dependencies": {
    "@cumbre/schemas": "workspace:*",
    "@cumbre/types": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "echo sdk lint scaffold pending",
    "test": "echo sdk test scaffold pending",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
