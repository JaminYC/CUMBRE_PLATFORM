{
  "name": "@cumbre/types",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "description": "Canonical domain entities for the CUMBRE platform",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "echo types lint scaffold pending",
    "test": "echo types test scaffold pending",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
