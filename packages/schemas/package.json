{
  "name": "@cumbre/schemas",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "description": "Shared API payload schemas for the CUMBRE platform",
  "dependencies": {
    "@cumbre/types": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "echo schemas lint scaffold pending",
    "test": "echo schemas test scaffold pending",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
