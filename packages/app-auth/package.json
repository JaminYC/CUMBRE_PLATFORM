{
  "name": "@cumbre/app-auth",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "description": "Shared signed app-session helpers for CUMBRE web applications",
  "dependencies": {
    "@cumbre/types": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "echo app-auth lint pending",
    "test": "echo app-auth test pending",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
