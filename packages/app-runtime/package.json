{
  "name": "@cumbre/app-runtime",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./client": "./src/client.ts"
  },
  "description": "Shared Next.js app runtime helpers for CUMBRE web applications",
  "dependencies": {
    "@cumbre/app-auth": "workspace:*",
    "@cumbre/schemas": "workspace:*",
    "@cumbre/types": "workspace:*",
    "next": "^15.3.1",
    "react": "^19.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/react": "^19.2.2",
    "typescript": "^5.7.2"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "echo app-runtime lint pending",
    "test": "echo app-runtime test pending",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
