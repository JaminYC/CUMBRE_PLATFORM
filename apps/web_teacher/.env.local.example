AUTH_SERVICE_URL=http://localhost:3001
LEARNING_SERVICE_URL=http://localhost:3002
CONTENT_SERVICE_URL=http://localhost:3003

# Sin definir a proposito: ver la nota en apps/web_student/.env.local.example.
# El valor bueno esta en lib/env.ts y coincide con la semilla de learning.
# NEXT_PUBLIC_DEFAULT_LEARNING_PATH_ID=

APP_SESSION_SECRET=cumbre-teacher-dev-session-secret
APP_SESSION_COOKIE_NAME=cumbre_teacher_session
APP_SESSION_COOKIE_DOMAIN=

# Acceso: por defecto se manda al portal de la institucion, que autentica
# y reparte por rol. El destino sale de la marca (paquete @cumbre/brands),
# no de una variable: una global mandaba a todos al portal de CUMBRE.
# Descomenta para volver al acceso propio de esta aplicacion.
# AUTH_USE_PORTAL=false
