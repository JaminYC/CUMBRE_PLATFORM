{
  "name": "@cumbre/recommendation-engine",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.ts",
  "dependencies": {
    "@cumbre/types": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "dev": "echo recommendation_engine dev scaffold pending",
    "lint": "echo recommendation_engine lint scaffold pending",
    "test": "echo recommendation_engine test scaffold pending",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
